{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "schema/railway/Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "NEVER",
    "startCommand": "./run-migrations.sh up"
  }
}
